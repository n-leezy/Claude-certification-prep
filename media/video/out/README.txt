Pre-rendered MP4s go here.

This folder is intentionally empty in the repo: the videos are rendered locally with
  npm install && npm run render:all
(see ../README.md). Rendering needs headless Chromium, which the original build
sandbox could not run to completion.
